apiVersion: v1
kind: Service
metadata:
  name: inventory
  labels:
    app: inventory
    service: inventory
spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 30080
    name: http
  selector:
    app: inventory
---